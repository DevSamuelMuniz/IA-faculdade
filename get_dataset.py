from pysus import SINAN

# sinan = SINAN().load()

# files_sinan = sinan.get_files(dis_code=["CANC"])

# files_sinan1 = sinan.get_files(dis_code="CANC", year=[2024, 2023])

# sinan.download(files_sinan1)
